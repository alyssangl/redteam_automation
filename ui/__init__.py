"""Terminal cockpit for watching the orchestrator drive/mutate an attack graph live.

Run it with:  python -m ui.cockpit   (or: python ui/cockpit.py)
See ui/README.md for the design (subprocess-isolated run + checkpoint polling).
"""
